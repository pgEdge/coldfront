/* coldfront extension upgrade 0.1 -> 1.0
 *
 * No schema or function changes: the 0.1 and 1.0 definitions are
 * identical. This script exists only so ALTER EXTENSION coldfront
 * UPDATE TO '1.0' succeeds for any installation that predates the
 * 1.0 version bump.
 */
